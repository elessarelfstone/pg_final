[Unit]
Description=etcd distributed reliable key-value store
After=network.target

[Service]
User=etcd
Type=notify
ExecStart={{ bin_directory }}/etcd --config-file {{ etc_directory }}/etcd.yml
Restart=always

[Install]
WantedBy=multi-user.target